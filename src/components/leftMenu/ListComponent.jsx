import React from "react";
import style from "./ListComponent.module.scss"

const ListComponent = ({image, text}) =>{
    return(<li className={style.root}>

    </li>);
};

export default ListComponent;